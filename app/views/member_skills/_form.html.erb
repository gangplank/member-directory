<%= form_for(@member_skill) do |f| %>
  <% if @member_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member_skill.errors.count, "error") %> prohibited this member_skill from being saved:</h2>

      <ul>
      <% @member_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.text_field :skill_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br>
    <%= f.text_field :member_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
